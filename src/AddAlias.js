import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class AddAlias extends Component {
  render() {
    return (
      <div>This will allow a user to add an alias</div>
    );
  }
}
