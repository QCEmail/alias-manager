import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class Admin extends Component {
  render() {
    return (
      <div>This will show the admin page for the user</div>
    );
  }
}
